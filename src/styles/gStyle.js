

const r_center = {
    flexDirection : 'row',
    justifyContent: 'center',
    alignItems: 'center',
}

const r_sb = {
    flexDirection : 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
}

const c_center = {
    flexDirection : 'column',
    justifyContent: 'center',
    alignItems: 'center',
}

const c_sb = {
    flexDirection : 'column',
    justifyContent: 'space-between',
    alignItems: 'center',
}

const box_shadow = {
    shadowColor: "#000",
    shadowOffset: {
      width: 0,
      height: 3,
    },
    shadowOpacity: 0.27,
    shadowRadius: 4.65,
    elevation: 6,
}

export default {
    r_center,
    r_sb,
    c_center,
    c_sb,
    box_shadow
}